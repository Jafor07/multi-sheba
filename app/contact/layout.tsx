import type { Metadata } from "next";

export const metadata: Metadata = { title: "যোগাযোগ | Multi Sheba", description: "Multi Sheba-এর সঙ্গে যোগাযোগ করুন।" };

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}